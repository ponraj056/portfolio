import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Ponraj D — Frontend Developer & Java Programmer",
  description:
    "Portfolio of Ponraj D, a frontend developer and Java programmer building fast, accessible web apps, Selenium test suites, and Gen AI‑powered demos.",
  keywords:
    "Ponraj D, Frontend Developer, Java Programmer, React, TypeScript, Selenium, Test Automation, Gen AI, India",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
